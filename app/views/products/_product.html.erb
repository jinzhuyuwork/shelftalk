<div id="<%= dom_id product %>" class="w-full sm:w-auto my-5 space-y-5">
  <div>
    <%= link_to product.name, product, class: "font-bold text-lg block font-medium mb-1 underline" %>
  </div>
  <div>
    <%= product.description %>
  </div>
</div>
